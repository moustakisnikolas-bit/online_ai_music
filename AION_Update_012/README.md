# AION Update 012

Adds the first usable web interface for the AION MVP.

## Adds

- FastAPI-served web UI
- Audio generation form
- Mode selector
- Preset selector
- Binaural and isochronic fields
- Audio preview
- Metadata display
- Download link
- UI route tests
- MVP completion notes

## Suggested commit

```bash
git add .
git commit -m "feat(ui): add first usable audio generation interface"
git push
```
