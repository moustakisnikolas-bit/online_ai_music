fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
sqlalchemy>=2.0,<3.0
psycopg[binary]>=3.2,<4.0
alembic>=1.14,<2.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0
redis>=5.2,<6.0
httpx>=0.28,<1.0
pillow>=11.0,<12.0
numpy>=2.0,<3.0
scipy>=1.14,<2.0
pyloudnorm>=0.1,<1.0
# Requires the native libfluidsynth library installed separately from pip
# (apt install fluidsynth on Linux; conda-forge or a manual DLL on Windows)
# -- see docs/15-synthesizer for setup. First native-library dependency in
# this project; synthesizer mode degrades to a clear error without it.
pyfluidsynth>=1.3,<2.0
google-api-python-client>=2.150,<3.0
google-auth>=2.35,<3.0
google-auth-oauthlib>=1.2,<2.0
cryptography>=43.0,<50.0
# libsndfile wrapper, used only by the Freesound importer to decode
# downloaded OGG previews into WAV -- the rest of the audio pipeline reads
# WAV via stdlib `wave` and doesn't need this.
soundfile>=0.12,<1.0
# Local speech-to-text (CTranslate2 reimplementation of Whisper -- lighter
# than openai-whisper, which pulls in a full PyTorch install). Backs the
# dormant captioning.py service: nothing in this codebase produces speech
# yet, so nothing calls it today, but it's ready for future narrated
# content. Downloads a model checkpoint on first real use.
faster-whisper>=1.0,<2.0
# Python's zoneinfo needs a real IANA tz database, which python:3.12-slim
# (the production image) doesn't ship -- only present today as an
# incidental transitive dependency of psycopg, not guaranteed. Pinned
# directly since the album pipeline's quota ledger relies on correct
# Pacific-Time day rollover (YouTube's quota resets at Pacific midnight).
tzdata>=2025.1
