# AION Update 013

Adds preset discovery and optional encoded audio export.

## Adds

- Preset catalog API
- Dynamic preset loading in the web UI
- Output format selection
- WAV, FLAC and MP3 response support
- FFmpeg availability detection
- Safe subprocess execution
- Encoding service tests
- Preset API tests
- Updated operations documentation

## Suggested commit

```bash
git add .
git commit -m "feat(audio): add preset catalog and optional FLAC MP3 export"
git push
```
