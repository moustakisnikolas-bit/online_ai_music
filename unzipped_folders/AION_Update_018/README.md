# AION Update 018

Adds real MP4 rendering and complete export bundle generation.

## Adds

- FFmpeg static-artwork video rendering
- MP4 render endpoint
- Export bundle generator
- Catalog manifest
- SHA-256 checksums
- ZIP packaging
- Safe filename validation
- Tests and documentation

## Suggested commit

```bash
git add .
git commit -m "feat(export): add MP4 rendering and catalog bundles"
git push
```
