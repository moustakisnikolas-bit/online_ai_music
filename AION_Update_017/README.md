# AION Update 017

Adds the first visual asset package for the AION MVP.

## Adds

- Procedural cover-art generator
- YouTube thumbnail generator
- Platform size presets
- Deterministic visual seeds
- PNG generation with Pillow
- Visual package API
- Video render manifest skeleton
- Tests and documentation

## Suggested commit

```bash
git add .
git commit -m "feat(visuals): add artwork and video package generation"
git push
```
